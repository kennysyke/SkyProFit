import { LoginForm } from "../../components/form/loginForm";
import * as Styled from "./styles";

export const Login = () => {     
    return (
      <Styled.Wrapper>
        <LoginForm/>   
      </Styled.Wrapper>
    );
  }